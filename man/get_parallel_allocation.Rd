% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parallel_utils.R
\name{get_parallel_allocation}
\alias{get_parallel_allocation}
\title{Calculate optimal parallel allocation for simulation studies}
\usage{
get_parallel_allocation(total_cores, num_repetitions, n_starts)
}
\arguments{
\item{total_cores}{Total cores available (from get_conservative_cores)}

\item{num_repetitions}{Number of simulation repetitions to run}

\item{n_starts}{Number of starting points per estimation}
}
\value{
List with sim_workers and cores_per_sim allocation
}
\description{
Calculate optimal parallel allocation for simulation studies
}
\details{
Determines the optimal allocation between simulation-level and estimation-level
parallelization based on available resources and workload characteristics.

Strategy selection logic:
\itemize{
\item Calculates potential throughput for both simulation-level and estimation-level parallelization
\item Chooses simulation-level when num_repetitions provides better parallelization opportunity
\item Chooses estimation-level when n_starts provides better parallelization opportunity
\item Always avoids nested parallelization (coordinated mode)
}

The function ensures efficient core utilization while maintaining simple,
single-level parallelization to avoid resource conflicts.
}
\examples{
# Typical usage in simulation study
total_cores <- get_conservative_cores(reserve_cores = 2)
allocation <- get_parallel_allocation(total_cores, num_repetitions = 50, n_starts = 3)

# Result interpretation:
# allocation$sim_workers = 1, cores_per_sim = 8  -> Estimation-level parallelization
# allocation$sim_workers = 8, cores_per_sim = 1  -> Simulation-level parallelization
# allocation$sim_workers = 1, cores_per_sim = 1  -> Sequential (no parallelization)
}
