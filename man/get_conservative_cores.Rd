% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parallel_utils.R
\name{get_conservative_cores}
\alias{get_conservative_cores}
\title{Parallel Computing Utilities}
\usage{
get_conservative_cores(max_cores = NULL, reserve_cores = 2, min_cores = 1)
}
\arguments{
\item{max_cores}{User-specified maximum cores (NULL for auto-detection)}

\item{reserve_cores}{Number of cores to reserve for system (default: 2)}

\item{min_cores}{Minimum cores to use even if detection fails (default: 1)}
}
\value{
Conservative number of cores to use safely
}
\description{
This file provides utilities for conservative parallel processing across
all simulation studies. Functions handle core detection, allocation strategy,
and graceful fallback when parallel packages are unavailable.
}
\details{
Main functions:
\itemize{
\item get_conservative_cores(): Detect cores with conservative reservation
\item get_parallel_allocation(): Determine optimal worker and core allocation
}

Dependencies: parallel package (graceful fallback if unavailable)
Get conservative core count for parallel processing

Implements a conservative approach to core allocation that:
\itemize{
\item Reserves cores for system use to avoid oversubscription
\item Handles edge cases where core detection fails
\item Never exceeds available cores
\item Provides sensible fallbacks
}

The reserve_cores parameter helps avoid the "200\% CPU load" warnings
that occur when parallel workers compete for limited resources.
}
\examples{
# Auto-detect with 2 cores reserved for system
cores <- get_conservative_cores()

# Manual limit with 1 core reserved
cores <- get_conservative_cores(max_cores = 4, reserve_cores = 1)

# Force single core (useful for debugging)
cores <- get_conservative_cores(max_cores = 1, reserve_cores = 0)
}
