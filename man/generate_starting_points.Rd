% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utility_functions.R
\name{generate_starting_points}
\alias{generate_starting_points}
\title{Generate diverse starting points for regime-switching model estimation}
\usage{
generate_starting_points(
  y,
  K,
  model_type = c("constant", "tvp", "exogenous", "gas"),
  n_starts,
  diag_probs = TRUE,
  equal_variances = FALSE,
  seed = NULL
)
}
\arguments{
\item{y}{Observed time series data}

\item{K}{Number of regimes}

\item{model_type}{Type of model ("constant", "tvp", "exogenous", "gas")}

\item{n_starts}{Number of starting points to generate}

\item{diag_probs}{If TRUE, use diagonal transition probability parameterization}

\item{equal_variances}{If TRUE, use single shared variance parameter}

\item{seed}{Random seed for reproducibility (optional)}
}
\value{
List of parameter vectors with proper attributes, each suitable for the specified model type
}
\description{
Generate diverse starting points for regime-switching model estimation
}
\details{
Generates diverse starting points for maximum likelihood estimation to help
avoid local optima. Uses data-driven heuristics to create reasonable initial
guesses while adding random variation for diversity.

UPDATED to support both diagonal and off-diagonal transition probability
parameterizations, as well as equal variance constraints. All returned
parameter vectors have proper attributes set for robust handling.

Strategy:
\itemize{
\item Means: Based on data quantiles with random noise
\item Variances: Fractions of data variance (single if equal_variances=TRUE)
\item Transition probabilities: Generated according to diag_probs setting
\item A coefficients: Random values in [0, 0.5] for moderate sensitivity
\item B coefficients: Random values in [0.5, 0.99] for persistence
}
}
\examples{
# Generate starting points for diagonal parameterization (original style)
y <- rnorm(200)
starts_diag <- generate_starting_points(y, K=2, model_type="constant",
                                         n_starts=3, diag_probs=TRUE)

# Generate starting points for off-diagonal parameterization (new style)
starts_offdiag <- generate_starting_points(y, K=3, model_type="tvp",
                                            n_starts=3, diag_probs=FALSE)
}
