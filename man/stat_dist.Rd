% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transition_helpers.R
\name{stat_dist}
\alias{stat_dist}
\title{Calculate the stationary distribution of a transition matrix}
\usage{
stat_dist(
  probs,
  diag_probs = TRUE,
  method = c("eigen", "power"),
  tol = 1e-08,
  fallback_value = NULL
)
}
\arguments{
\item{probs}{Vector of probability parameters OR transition matrix}

\item{diag_probs}{If probs is a vector, whether it uses diagonal parameterization}

\item{method}{Method for calculation ("eigen" or "power")}

\item{tol}{Tolerance for eigenvalue detection (default: 1e-8)}

\item{fallback_value}{Value to return if no unique stationary distribution exists}
}
\value{
The stationary distribution as a vector
}
\description{
Calculate the stationary distribution of a transition matrix
}
\details{
Calculates the stationary distribution π such that π = π⋅P, where P is the transition matrix.

Two methods available:
\itemize{
\item "eigen": Uses eigendecomposition (faster, default)
\item "power": Uses power iteration (more stable for ill-conditioned matrices)
}
}
\examples{
# Using diagonal parameters
stat_dist(c(0.8, 0.9), diag_probs = TRUE)

# Using off-diagonal parameters  
stat_dist(c(0.2, 0.1), diag_probs = FALSE)

# Using transition matrix directly
P <- transition_matrix(c(0.8, 0.9), diag_probs = TRUE)
stat_dist(P)
}
